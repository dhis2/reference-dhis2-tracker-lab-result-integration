{
  "name": "reference-dhis2-tracker-lab-result-integration",
  "version": "1.0.0",
  "main": "index.js",
  "keywords": [],
  "author": "",
  "license": "BSD-3-Clause",
  "devDependencies": {
    "@playwright/test": "^1.47.2",
    "@types/node": "^22.7.3",
    "concurrently": "^9.0.1",
    "wait-on": "^8.0.1"
  },
  "scripts": {
    "build-iol": "mvn -B clean package -Dmaven.test.skip=true -f iol/pom.xml",
    "build": "concurrently --kill-others-on-fail \"yarn:build-iol\"",
    "start-services": "docker compose up --build --renew-anon-volumes --force-recreate --remove-orphans",
    "start": "concurrently --kill-others-on-fail \"yarn:start-services\"",
    "test": "playwright test"
  }
}
